@model VegListViewModel
<h1>@Model.currCategory</h1>
<div class="row mt-5 mb-2">
    @{
        foreach (Veg veg in Model?.allVegs)
        {
            @Html.Partial("AllVegs", veg);
        }
    }
</div>